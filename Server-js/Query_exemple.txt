query {
  users {
    id
    name
  }
}

query {
  users {
    ...UserInfo
  }
}
fragment UserInfo on User {
  id
  name
}

query {
  events {
    id
    title
  }
}

mutation($name: String!, $password: String!) {
  createUser(name: $name, password:$password) {
    id
    name
  }
}

mutation($name: String!) {
  login(name: $name, password: "Lace")
}

Header :

Athorization : Bearer {le token retoutner par la mutation login}

mutation($title: String!, $start: String!, $end: String!, $organizerId: ID!) {
  createEvent(title: $title, start: $start, end: $end, organizerId: $organizerId){
    id,
    title,
    dateRange {
      start,
      end
    }
    organizer {
      id
    }
  }
}



