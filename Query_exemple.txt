query {
  users {
    id
    name
  }
}

query {
  users {
    ...UserInfo
  }
}
fragment UserInfo on User {
  id
  name
}

query {
  events {
    id
    title
  }
}

mutation($name: String!) {
  createUser(name: $name) {
    id
    name
  }
}

mutation($title: String!, $start: String!, $end: String!, $organizerId: ID!) {
  createEvent(title: $title, start: $start, end: $end, organizerId: $organizerId){
    id,
    title,
    dateRange {
      start,
      end
    }
    organizer {
      id
    }
  }
}



